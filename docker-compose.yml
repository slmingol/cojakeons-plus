services:
  conjakeons-plus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: conjakeons-plus
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - conjakeons-network

networks:
  conjakeons-network:
    driver: bridge
