services:
  conjakeons-plus-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: conjakeons-plus-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    networks:
      - conjakeons-network
    stdin_open: true
    tty: true

networks:
  conjakeons-network:
    driver: bridge
